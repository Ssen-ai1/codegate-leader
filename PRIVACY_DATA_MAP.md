# CodeGate Leader 隐私与数据流地图

本文是法务起草隐私政策和工程审计的事实底稿，不是已经批准的隐私政策。

| 数据 | 保存位置 | 何时离开设备 | 保护与删除 |
|---|---|---|---|
| 项目文件、TaskSpec、架构、Plan、Handoff、报告 | 用户选择的项目目录 `.codegate/` | 默认不离开设备 | 用户控制项目目录；事件链与备份用于恢复 |
| API Key | Electron 用户数据目录中的设置文件 | 仅作为模型请求鉴权头发送给用户配置的模型服务 | Windows `safeStorage` 加密；可在设置中清除；诊断不导出明文 |
| 模型请求上下文 | 内存 | 用户主动咨询、生成或审查时，发送到用户配置的 API 地址 | 发送前采用产品既有上下文边界；服务商留存政策由用户选择的服务商决定 |
| 模型用量 | 项目 `.codegate/` 本地指标 | 当前不上传 | 仅记录调用次数、Token 与按用户价格估算的成本，不记录 Key |
| 订阅 Token | Electron 用户数据目录中的设置文件 | 发送到用户配置或正式内置的授权服务 | Windows `safeStorage` 加密；可清除；不进入诊断 |
| 签名授权缓存 | Electron 用户数据目录 | 不上传；由服务端响应生成 | 只接受签名、绑定安装实例且在离线期限内的载荷 |
| 安装实例 UUID | Electron 用户数据目录 | 授权检查请求头 | 不等同账号身份；重装或清除用户数据后可变化 |
| 产品过程指标 | 项目本地 | 当前不上传 | 记录对话、审查、计划修订、验证和接受步骤数量 |
| 更新检查元数据 | 内存 | 发送给配置的更新源：平台、架构、当前版本 | 不发送项目内容、Key 或订阅 Token |
| 诊断文件 | 用户选择的导出位置 | 仅由用户自行分享 | 包含版本、平台、公开设置、自检与日志路径；不包含秘密凭据 |
| 错误日志 | Electron 用户数据目录 `logs/main.log` | 当前不上传 | 用于本机诊断；可能包含错误消息，发布前需进行额外隐私审计 |

## 默认原则

- 本地优先；当前没有静默遥测上传。
- 外部模型调用必须由用户配置服务并主动触发。
- 秘密凭据不写入项目目录、不进入导出的诊断文件。
- 若未来增加云同步、账号分析或崩溃上报，必须先更新本表、取得同意并完成法律审查。

