# 安全政策

## 支持范围

| 版本 | 状态 |
|---|---|
| `0.2.x-alpha` | 接收安全报告，按风险尽力修复 |
| 更早版本 | 不再维护 |

当前项目处于 Alpha。它不应被视为已经通过正式第三方安全审计的商业产品。

## 报告漏洞

请不要把漏洞细节、真实 API Key、许可证 Token、私有源码或用户目录公开到 Issue。

推荐通过 GitHub 仓库的 **Security → Report a vulnerability** 创建私密安全公告草稿：

`https://github.com/Ssen-ai1/codegate-leader/security/advisories/new`

报告请包含受影响版本和系统、漏洞影响、最小复现步骤、所需模型/文件/Agent、已知缓解方式，以及可公开的脱敏日志或 PoC。

## 重点安全边界

我们尤其关注：

- API Key、Token 或私钥泄露；
- Renderer 到主进程的 IPC 越权；
- 项目根目录之外的任意文件读写；
- 路径穿越和恶意附件；
- 命令注入或未经确认的外部执行；
- 模型 Prompt Injection 绕过领域门禁；
- 许可证签名绕过；
- 更新源劫持和哈希验证缺陷；
- `.codegate` 工件、事件链或验证结果伪造。

## 用户安全建议

- 使用最小权限 API Key，并定期轮换；
- 不要把秘密写入任务资料、Prompt、截图或执行报告；
- 在运行 Agent 或验证命令前阅读操作范围；
- 只从可信来源获取安装包；
- 当前未签名 Alpha 建议从源码运行或自行构建；
- 分享诊断前检查其中的项目路径和业务信息。
