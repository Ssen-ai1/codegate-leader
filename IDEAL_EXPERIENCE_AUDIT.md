# CodeGate Leader 理想体验阶段验收

版本：0.2.0-alpha.7  
日期：2026-08-24

## 本轮产品目标

把 alpha4 的“可完成工作流”提升为普通产品用户能够理解、能够恢复、能够持续讨论的体验。正常模式不要求用户理解 TaskSpec、ADR、WorkPlan、Handoff 或 Execution Report 的内部结构；这些工件继续作为高级模式和可信审计的底层协议存在。

## 已完成的 P0 体验闭环

| 用户问题 | alpha7 的处理 |
| --- | --- |
| 我没有任务资料，怎样开始？ | 从一句产品想法进入逐题需求对话，回答实时沉淀到产品蓝图。 |
| 我不知道需求是否完整 | 蓝图显示产品目标、目标用户、MVP、平台、数据与集成、成功标准、商业限制七类完整度和明确缺口。 |
| 为什么选择这个架构？ | 同屏比较三个方案的交付速度、成本、风险、适用场景、优势与代价；选择后写入不可覆盖的 ADR。 |
| 计划到底怎样推进？ | 使用可扫描的步骤地图显示顺序、依赖、状态与验收映射；未执行步骤仍可通过 PlanPatch 修订。 |
| Agent 当前做到哪里？ | 执行中心明确显示“准备 Prompt → Agent 工作 → 证据报告”三个状态，并集中提供 Prompt、报告检测和摘要。 |
| Leader 的回答为什么消失？ | 每个项目保存最近 100 条用户/Leader 消息，关闭或刷新后恢复；敏感令牌在保存和调用模型前被脱敏。 |
| 没有 API Key 是否不可用？ | 右侧明确显示“本地引导 · 可离线”，仍能解释下一步、架构取舍、计划风险和当前步骤产出；配置模型后切换为模型咨询。 |
| 修改前面阶段会发生什么？ | 自定义影响预览列出停止执行、计划失效、版本创建和历史保留，再由用户确认。 |
| 为什么页面出现 JSON？ | 普通模式只显示对话、摘要、卡片、地图和状态；原始工程工件仅在高级模式或 Agent Prompt 中查看。 |
| 怎样恢复已有项目？ | 最近项目显示项目名、当前阶段、最后活动日期和缺失目录状态。 |

## 可信性与兼容性

- 产品定义、架构、计划、Prompt、报告、验证和 Review 的版本化与不可覆盖规则保持不变。
- 现有 alpha4 工作区无需迁移；`assistant/history.json` 在首次咨询时增量创建。
- 当前 Prompt 和对应报告进入快照，执行中心不会把其他 Handoff 版本的报告误当成当前结果。
- 对话历史限制为最近 100 条，避免项目状态无限增长。
- Renderer 仍处于 Electron `sandbox`、`contextIsolation` 和受限 IPC 边界内。

## alpha6 首次使用与商业成本体验

- 首页首次出现三步工作方式说明，用户确认后本机记忆，不在每次启动重复干扰。
- 新建项目提供空白产品、可销售桌面 App、订阅制 Web SaaS、内部效率工具四类模板；模板只补充平台、约束和描述示例，不直接制造未经用户确认的需求。
- Agent 执行前通过系统 PATH 只读检测 Codex 与 Claude Code。已安装时用户可显式“复制并打开 Agent”；应用打开当前项目下的交互式终端，但不静默执行 Prompt 或业务代码。
- 支持按实际服务商账单配置输入/输出每百万 Token 价格。模型响应中的 Token 使用量以项目为边界保存，并显示累计 Token 和估算 USD；价格为零时不伪造金额。
- 讨论数、Review 数、已验收步骤等指标从当前项目工件本地聚合，不上传遥测，也不包含任务正文。
- 原创应用标记以 “C 形入口 + 验证勾 + 交付路径” 表达产品定位，已生成 PNG 与多尺寸 Windows ICO，并用于窗口、应用 EXE 和安装包。

## alpha7 订阅、更新与隐私体验

- 顶栏增加账号与订阅入口，清楚区分未配置、试用、有效、离线宽限、过期、撤销和不可验证状态。
- 授权只接受服务端 Ed25519 签名结果，并绑定当前安装实例；断网时只在签名载荷规定的期限内使用缓存。
- 授权 Token 与 API Key 一样进入 Windows 安全存储，不进入项目目录或诊断文件。
- 检查更新使用独立对话框显示版本、说明和 SHA-256；用户明确点击后才打开 HTTPS 下载地址，Alpha 不静默安装。
- 本地数据、模型服务、授权服务、更新检查与诊断导出的数据流已形成隐私数据地图。

## 验收证据

- `npm run check`：14 个测试文件、53 项测试通过。
- Workflow 新增覆盖：蓝图缺口与 100% 完整度、三种架构方案唯一推荐、离线架构解释、项目级对话恢复。
- Electron E2E 新增覆盖：对话刷新恢复、架构卡片、计划地图、三段执行中心、变更影响对话框。
- 视觉验收：`alpha7-welcome.png`、`alpha7-project-template.png`、`alpha7-architecture.png`、`alpha7-plan.png`、`alpha7-execution.png`。

## 尚未伪装完成的商业边界

公开收费发行仍需要真实的法律主体、Windows 代码签名证书、律师批准的 EULA/隐私政策、生产 HTTPS 更新源和生产订阅后端。客户端协议、验签、离线宽限、数据地图与发布门已经完成；alpha7 不通过本地假数据绕过外部条件。
