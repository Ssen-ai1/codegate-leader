# CodeGate Leader 产品与技术介绍

> **先把产品想清楚，再让 Agent 写代码。**  
> 当前版本：`0.2.0-alpha.9` · Windows 开源 Alpha · Workspace Protocol v2

---

## 1. 产品简介

CodeGate Leader 是一个与 Coding Agent 解耦的 AI 技术负责人工作台。

它不与 Codex、Claude Code 等工具竞争代码生成能力，而是在它们之上建立一层稳定的产品与工程管理能力：从一句产品想法或一个已有代码仓库出发，帮助用户补齐需求、比较架构、制定开发计划、生成 Agent Prompt，并根据真实代码变化、测试结果和执行证据进行独立审查与纠偏。

```text
产品想法 / 任务资料 / 已有工程
        ↓
需求访谈与产品蓝图
        ↓
TaskSpec 与架构决策
        ↓
动态开发计划与验收映射
        ↓
Agent Prompt / Handoff
        ↓
Codex、Claude Code 或其他 Agent 执行
        ↓
执行报告、真实 Diff 与验证证据
        ↓
独立 Review、纠偏、用户决策与完成交付
```

CodeGate Leader 提供两种共享同一可信工程内核的体验：面向创业者和开发团队的“产品开发模式”，以及面向 FPGA/嵌入式竞赛队伍的“竞赛冲刺模式”。前者从产品想法出发，后者从官方赛题、板卡与得分项出发。

---

## 2. CodeGate Leader 解决什么问题

直接使用 Coding Agent 时，用户经常遇到以下问题：

- 一开始只有模糊想法，不知道如何建立完整需求。
- Agent 很快开始写代码，但产品目标、非目标和验收标准尚未确定。
- 对话压缩、会话重启或更换模型后，关键决定容易丢失。
- 长任务执行中，Agent 可能扩大范围、偏离架构或遗漏交付物。
- “代码已经生成”容易被误认为“产品已经验收”。
- 用户不知道当前处于哪一步，也不知道下一步应该做什么。
- Codex、Claude Code 等 Agent 之间缺少中立、稳定的交接协议。
- 技术解释和执行指令混在一起，既影响执行效率，也不利于用户理解。

CodeGate Leader 将这些问题转化为可持续维护的任务事实、架构决策、步骤计划、交接协议和审查证据。

---

## 3. 已实现的核心功能

### 3.1 从零创建新产品

用户不需要提前准备需求文档，只需描述一句产品想法即可开始。

CodeGate 会通过逐题访谈补齐：

- 产品目标与核心问题。
- 目标用户。
- MVP 范围与明确非目标。
- 运行平台。
- 数据、账号、支付和第三方集成边界。
- 可衡量的成功标准。
- 时间、成本、隐私、离线能力等商业限制。

首页提供四类起步模板：

- 空白产品。
- 可销售桌面 App。
- 订阅制 Web SaaS。
- 内部效率工具。

另提供独立的“FPGA / 嵌入式竞赛”入口，不要求学生填写目标用户、商业模式等产品访谈字段。

回答会实时沉淀为正式 TaskSpec，而不是只保存在聊天记录中。

### 3.2 打开并恢复已有工程

CodeGate 支持打开现有代码目录，并为没有 CodeGate 工件的工程建立第一份任务计划；如果目录中已经存在 `.codegate` 工作区，则会恢复原来的阶段、工件、对话和下一步。

打开时会自动执行：

- 未完成事务恢复。
- 工作区健康检查。
- State、Task、Plan、Handoff、Report 和事件链一致性检查。
- 损坏工件备份与可恢复修复。
- 未来协议版本降级保护。

最近项目列表显示项目名、当前阶段、最后活动时间和目录可用性。

### 3.2A FPGA / 嵌入式竞赛冲刺模式

竞赛模式针对“先读懂长篇赛题、再用有限时间建立可上板作品”的团队，使用学生可理解的六阶段界面：

```text
赛题拆解 → 实现路线 → 冲刺计划 → AI 执行 → 上板验收 → 提交答辩
```

已实现：

- 从项目外选择 PDF、DOCX、Markdown、文本或图片，安全复制并归档到项目。
- 从一份包含多个选题的指南中列出题目，让用户先选择本次挑战。
- 针对 FPGA CPU、视频/图像、FPGA+ARM AI、开放 SOPC 题目分类。
- 提取基础任务、高阶任务、板卡、PDS/编译器/测评工具、性能指标、现场要求和提交物。
- 基于官方赛题生成带 SourcePointer 的得分地图，而不是要求学生先理解 TaskSpec/Rubric。
- 确认目标板卡、工具链真实进度和保分/冲刺策略，禁止 Agent 猜测引脚、时钟、器件或 IP 参数。
- 针对题型比较三条实现路线，再生成“工具链—最小硬件闭环—基础分—高阶优化—现场鲁棒—提交答辩”六阶段计划。
- 对开放题主动询问应用场景、输入、核心处理、输出和现场演示方式，并将创新性、趣味性、实用性纳入评价地图。
- 记录 CoreMark、频率、资源、帧率、准确率等测量值及板卡、频率、输入数据和测试条件。
- 调试快车道识别编译、仿真、综合、布局布线、时序、约束、下载、硬件、资源和性能问题，生成局部修复 Prompt，不改写正式计划。
- 竞赛导师解释数据流、时序、资源和证据边界；提交阶段基于当前工程生成六类模拟答辩问题。

### 3.3 产品定义与蓝图完整度

产品定义界面展示七类蓝图状态：

1. 产品目标。
2. 目标用户。
3. MVP 与非目标。
4. 平台与运行环境。
5. 数据和外部集成。
6. 成功标准。
7. 商业与交付限制。

用户能够直观看到哪些信息已经明确、哪些信息仍然缺失。产品定义可以重新打开和版本化修订，旧版本不会被覆盖。

### 3.4 架构方案比较与决策记录

TaskSpec 获得确认后，CodeGate 会根据需求、平台和约束生成三个可比较的架构方向，并展示：

- 交付速度。
- 开发和运营成本。
- 技术风险。
- 适用场景。
- 主要优势。
- 代价与限制。
- Leader 推荐理由。

用户确认后的结果写入版本化 Architecture Decision Record。重新修改架构时，系统会先展示对现有 Plan 和 Prompt 的影响。

### 3.5 动态开发计划

CodeGate 根据 TaskSpec、交付物、验收标准、评分项、环境事实和架构决策生成 3～8 个可执行步骤。

每个 PlanStep 包含：

- 稳定步骤 ID。
- 当前目标和拆分理由。
- 前置依赖。
- 对应需求与交付物。
- 验收标准和评分项映射。
- 推荐 Skill 及选择理由。
- 预期输入与输出。
- 验证方法。
- 风险和停止条件。

计划批准前会检查循环依赖、无效引用和必需验收覆盖。执行中如需调整，会通过 PlanPatch 增量修订并保留原因，不静默重写已接受步骤。

### 3.6 Agent Prompt 与跨 Agent 交接

CodeGate 将当前步骤编译为一份范围受控、可直接交给 Coding Agent 的 Prompt，固定包含：

- 当前唯一目标。
- 相关需求与交付物。
- 已批准架构决策。
- 已知环境事实。
- 推荐方法与质量门槛。
- 范围和非目标。
- 预期输出。
- 验证要求。
- 停止条件。
- Execution Report Contract。

目前支持 Generic、Codex 和 Claude Code 适配方式。桌面端会检测本机是否安装 Codex 或 Claude Code；用户可以一键复制 Prompt，并在当前项目目录中打开对应交互式 Agent。

CodeGate 不会在用户不知情时自动执行 Prompt，也不会直接修改业务代码。

### 3.7 执行报告与真实结果审查

Agent 的“已经完成”只是一项声明，不等于验收通过。

CodeGate 可以导入结构化 Execution Report，并交叉检查：

- 当前 Handoff 基线。
- 交接后真实 Git Diff 或文件变化。
- Agent 声明的修改文件。
- 命令输出和日志附件。
- Requirement、Acceptance 与 Rubric 映射。
- 工作区环境事实。
- 架构和范围变化。

Review 会分别生成需求覆盖、验收覆盖、评分覆盖、架构、实现、验证和漂移 Findings。

### 3.8 可信验证

CodeGate 只执行用户明确确认、与环境事实白名单完全匹配且不含危险 Shell 元字符的验证命令。

验证记录会保存：

- 实际执行命令。
- 退出码。
- 标准输出与错误输出。
- 输出哈希。
- 对应 Handoff 和步骤。
- 执行时间。

Agent 自报日志、`echo passed` 或未经 CodeGate 执行的测试结果不能直接构成 Accepted 证据。

### 3.9 偏航检测、纠偏与用户决策

出现以下情况时，CodeGate 不会简单继续生成 Prompt：

- Agent 扩大了未经确认的范围。
- 实际实现改变了已批准架构。
- 报告与真实 Diff 不一致。
- 必需验收没有证据。
- 同一问题多次纠偏仍未解决。

系统会生成最小 Correction Handoff，或进入用户决策状态，让用户选择接受风险、请求纠偏、更新计划或阻塞任务。

### 3.10 Leader Assistant 与技术导师

右侧 Leader Assistant 会结合当前项目的 TaskSpec、架构、Plan、Review 和状态回答问题。

- 未配置模型时，仍提供确定性的本地状态解释和下一步引导。
- 配置模型后，可以进行产品、架构、计划、风险和 Review 讨论。
- 对话以项目为边界保存，刷新或重新打开后仍可恢复。
- Agent Brief 与 Mentor Brief 分离，教学内容不会污染执行 Prompt。
- 学习档案支持基础、进阶等层级以及解释深度设置。

### 3.11 多格式任务资料导入

当前支持：

- Markdown 和纯文本。
- CSV 与 JSON。
- PDF。
- DOCX。
- PNG、JPG 等图片 OCR。

系统可以提取需求、交付物、约束、假设、开放问题、验收标准和 Rubric 候选项，并通过 SourcePointer 保留文件、页码或行位置。

### 3.12 模型与成本管理

CodeGate 支持 OpenAI Chat Completions 兼容服务，可配置：

- API 地址。
- Leader 模型。
- 独立 Review 模型。
- 请求与验证超时。
- 输入/输出每百万 Token 价格。

模型调用按项目记录调用次数、Token 使用量和估算成本。单价为零时只显示 Token，不伪造金额。

### 3.13 账号、订阅与更新基础能力

桌面端已具备商业版本所需的客户端信任基础：

- API Key 与订阅 Token 使用 Windows `safeStorage` 加密。
- 授权状态只接受服务端 Ed25519 签名结果。
- 授权载荷绑定产品和当前安装实例。
- 断网时只在签名载荷规定的离线宽限期内使用缓存。
- 本地设置或本地文件不能伪造有效订阅。
- 更新检查只接受 HTTPS 清单和 HTTPS 下载地址。
- 更新界面展示版本、说明和 SHA-256。
- 用户明确点击后才打开系统下载页面，Alpha 不会静默安装。

生产授权后端、支付流程、代码签名证书和正式法律文本仍属于商业发布前的外部上线条件。

---

## 4. 用户体验流程

### 4.1 新建产品

```text
新建项目
  → 选择起步模板
  → 输入一句产品想法
  → CodeGate 逐题补齐需求
  → 确认产品定义
  → 比较并确认架构
  → 生成并批准开发计划
  → 生成 Agent Prompt
  → Agent 执行并提交报告
  → CodeGate 验证和独立 Review
  → 接受、纠偏或进入用户决策
```

### 4.2 已有工程

```text
打开已有项目
  → 检查并恢复工作区
  → 导入任务资料或描述本轮目标
  → 识别现有环境事实与代码边界
  → 建立 TaskSpec、架构和 Plan
  → 从当前步骤继续执行与验收
```

### 4.4 切换 Coding Agent

每一轮 Prompt 与 Execution Report 都通过中立工作区协议保存。用户可以在不同步骤选择 Codex、Claude Code 或其他 Agent，而无需重新解释完整历史。

### 4.3 竞赛冲刺

```text
新建竞赛项目
  → 导入官方赛题并选择具体题目
  → 确认板卡、工具链与团队当前进度
  → 审查基础分/高阶项/指标/提交物
  → 选择保底、平衡或高阶实现路线
  → 批准六阶段冲刺计划
  → 生成小步 Agent Prompt
  → 用调试快车道处理局部错误
  → 保存真实跑分与上板证据
  → 检查提交清单并模拟答辩
```

---

## 5. 产品创新点

### 5.1 AI 技术领导层，而不是另一个 Coding Agent

CodeGate 将价值集中在任务连续性、架构稳定性、执行交接和独立验收，而不是重复建设代码生成能力。

### 5.2 从模糊想法建立可执行产品事实

产品不是要求用户先准备一份专业 PRD，而是通过引导访谈将一句想法逐步转化为可追踪、可批准、可验收的 TaskSpec。

### 5.3 工件优先的跨会话记忆

关键事实进入版本化工件，而不是只存在模型对话中。即使关闭应用、重启会话或切换 Agent，任务目标、架构和计划仍然可恢复。

### 5.4 Agent Claim 与 Review Acceptance 分离

CodeGate 明确区分“Agent 报告完成”和“结果获得验收”，优先读取真实工作区变化和由 CodeGate 保存的验证证据。

### 5.5 受控的动态计划

Plan 可以根据新事实调整，但必须通过增量 PlanPatch 或显式用户确认。这样既避免僵化计划，也避免执行 Agent 静默改变目标。

### 5.6 执行与教学双通道

给 Agent 的 Prompt 保持简洁、范围受控；给用户的 Mentor Brief 负责解释原因、原理、替代方案和调试方法。

### 5.7 模型无关、Agent 无关

核心状态、协议和验收逻辑不依赖特定模型供应商。用户可以配置不同的 Leader 和 Reviewer 模型，也可以在 Codex、Claude Code 与其他 Agent 之间切换。

### 5.8 可恢复、可审计的本地工作区

原子写入、事务恢复、不可覆盖历史、SHA-256 事件链和备份修复，使复杂任务不依赖一次性聊天会话保持正确状态。

### 5.9 双体验、同一可信内核

学生看到的是得分地图、下一步、调试与答辩；专业团队仍能在高级模式查看 TaskSpec、ADR、Plan、Handoff、Review 等正式工件。体验可以降维，事实、版本与证据标准不会降级。

---

## 6. 技术架构

```text
┌──────────────────────────────────────────────────────┐
│ Electron Desktop UI                                  │
│ 新建/恢复 · 产品蓝图 · 架构比较 · Plan · Review · Mentor │
└───────────────────────┬──────────────────────────────┘
                        │ 受限 IPC / Context Isolation
┌───────────────────────▼──────────────────────────────┐
│ Leader Workflow                                      │
│ Intake · State Machine · Planner · Handoff · Review  │
└─────────────┬─────────────────────────┬──────────────┘
              │                         │
┌─────────────▼──────────────┐ ┌────────▼────────────────┐
│ Workspace Protocol v2     │ │ Optional Model Service  │
│ Task/ADR/Plan/Report/Event│ │ Leader / Independent    │
│ Baseline/Review/Correction│ │ Reviewer / Mentor       │
└─────────────┬──────────────┘ └─────────────────────────┘
              │
┌─────────────▼────────────────────────────────────────┐
│ Codex / Claude Code / Other Coding Agent             │
│ 探索仓库 · 编写代码 · 运行测试 · 输出 Execution Report │
└──────────────────────────────────────────────────────┘
```

### 6.1 主要技术栈

- TypeScript 6。
- Node.js 22+。
- Electron 40。
- Zod Schema 校验。
- Vitest 自动化测试。
- electron-builder / NSIS Windows 打包。
- PDF、DOCX 与 OCR 按需解析。

### 6.2 本地工作区结构

```text
.codegate/
├── manifest.json
├── state.json
├── task/
├── architecture/decisions/
├── plan/
├── skills/
├── handoffs/
├── baselines/
├── agent-reports/
├── verification/
├── reviews/
├── corrections/
├── decisions/
├── environment/
├── assistant/
├── learning/
├── competition/debug/
├── competition/metrics/
├── competition/defense/
├── backups/
└── events.jsonl
```

### 6.3 可靠性设计

- 关键工件原子写入。
- 多工件变更使用可重放事务。
- 工件按版本保存，不覆盖历史。
- 工作区事件使用 SHA-256 哈希链。
- 打开项目时执行健康检查和自动恢复。
- Git 与非 Git 工程均可建立交接基线。
- `.codegate` 管理文件不会被错误计入业务实现 Diff。

---

## 7. 安全与隐私边界

- Electron Renderer 启用 Sandbox 与 Context Isolation。
- Renderer 只能通过白名单 IPC 调用本机能力。
- 项目目录必须先通过系统目录选择器授权。
- 验证命令必须由用户确认，并严格匹配白名单。
- 模型输入、日志和诊断采用结构化敏感信息脱敏。
- API Key 与订阅凭据不写入项目目录。
- 当前没有静默遥测上传。
- 项目内容默认只保存在用户本地工作区。
- 外部模型调用只发送到用户明确配置的服务地址。
- 更新和授权正式环境要求 HTTPS。

详细数据流见 `PRIVACY_DATA_MAP.md`。

---

## 8. 适用用户和场景

### 适合

- 使用 AI 编程工具但不擅长建立产品需求和开发计划的创业者。
- 需要长期维护任务上下文的个人开发者。
- 需要在 Codex、Claude Code 等工具之间切换的用户。
- 有明确评分标准和多项交付物的竞赛项目。
- 需要控制范围、架构和验收质量的小型软件团队。
- 希望在完成项目的同时理解技术原理的初学者。

### 当前不定位为

- 自动替用户编写全部代码的 Coding Agent。
- 无人值守的多 Agent 调度平台。
- 通用 IDE 或语言服务器。
- 企业级多人权限和云端项目管理系统。
- 不经用户授权即可执行外部操作的自动化工具。

---

## 9. 验证状态

截至 `0.2.0-alpha.8`：

- 15 个自动化测试文件。
- 58 项协议、工作流、安全、恢复、模型、CLI 和 Electron E2E 测试通过。
- 生产依赖安全审计发现 0 个已知漏洞。
- Windows NSIS 安装包构建成功。
- 打包态 ASAR 工作流、事件链和 Windows 安全存储烟雾测试通过。
- 首页、账号、产品模板、竞赛模板、竞赛冲刺中心、得分地图和调试快车道完成视觉验收。

这些证据证明当前软件功能能够进入持续内部试用，但不能替代真实用户价值对照测试，也不代表公开收费发行所需的法律和运营条件已经完成。

---

## 10. 安装与使用

### 系统要求

- Windows 10/11 x64。
- 使用安装包时无需单独安装 Node.js。
- 若需要调用 AI 模型，用户需准备兼容服务的 API Key。
- 若要直接打开 Codex 或 Claude Code，需要先安装对应命令行工具。

### 首次使用建议

1. 安装并启动 CodeGate Leader。
2. 选择“新建项目”或“打开已有项目”。
3. 产品项目描述一句想法并完成引导访谈；竞赛项目导入官方赛题并选择具体题目。
4. 确认产品定义、架构和开发计划。
5. 生成 Agent Prompt，并交给已安装的 Coding Agent。
6. Agent 完成后导入 Execution Report。
7. 使用 CodeGate 执行可信验证与独立 Review。
8. 根据 Review 结果接受步骤、生成纠偏或更新计划。

---

## 11. 商业发布状态

当前 Alpha 安装包用于内部测试和产品验证，尚未作为正式收费版本公开发布。

软件侧已经具备：

- 原创产品图标。
- 订阅客户端验签协议。
- 离线授权宽限处理。
- 更新服务接口。
- 隐私数据地图。
- 严格商业发布检查。

正式公开发行仍需落实：

- 可核验的发行商法律实体。
- Windows Authenticode 代码签名证书。
- 经法律顾问批准的 EULA 与隐私政策。
- 生产授权、账号、购买、退款和恢复服务。
- 正式 HTTPS 更新服务、域名、监控和运营流程。

CodeGate 的发布门会在这些条件未完成时拒绝将测试版本标记为可公开收费版本。

---

## 12. 推广用简短介绍

### 一句话版本

> CodeGate Leader 是 Codex、Claude Code 等 AI 编程工具之上的技术负责人工作台，把产品想法转化为可执行计划，并用真实代码变化和验证证据完成独立验收。

### 标准介绍

> 有了 AI 编程工具，写代码越来越快，但把产品想清楚、保持架构稳定、让多个 Agent 连续协作并真正验收结果，仍然很难。CodeGate Leader 从一句想法或已有工程开始，帮助用户建立产品蓝图、比较技术架构、拆解开发计划、生成 Agent Prompt，并根据真实 Diff、测试和执行报告发现偏航与遗漏。它不替代 Codex 或 Claude Code，而是让这些 Agent 在长期复杂项目中工作得更稳定、更可控、更容易验收。

### 核心价值主张

> 从“让 AI 写代码”升级为“让 AI 按正确目标、正确架构和可验证计划完成产品”。

---

## 13. 相关技术资料

- `README.md`：开发和使用入口。
- `ALPHA_TECHNICAL_HANDOFF.md`：当前技术交接。
- `IDEAL_EXPERIENCE_AUDIT.md`：用户体验验收。
- `STABILITY_AUDIT.md`：稳定性与安装包证据。
- `LICENSE_SERVICE_CONTRACT.md`：授权服务协议。
- `UPDATE_SERVICE_CONTRACT.md`：更新服务协议。
- `PRIVACY_DATA_MAP.md`：隐私与数据流地图。
- `COMMERCIAL_RELEASE_RUNBOOK.md`：商业发布运行手册。

---

**CodeGate Leader：复杂任务的稳定事实源、技术架构与计划管理器、跨 Agent 交接协议、独立审查者和技术导师。**
