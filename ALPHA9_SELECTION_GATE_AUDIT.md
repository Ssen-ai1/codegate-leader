# CodeGate Leader 0.2.0-alpha.9 赛题身份门禁审计

## 结论

测试项目中的 CodeGate **不知道用户正在做“赛题几”**。旧版 TaskSpec 只保存了整本竞赛指南名称，却仍进入 `plan-ready`，因此此前出现的 CPU、Cache、AI 加速路线不具备赛题事实依据。

`0.2.0-alpha.9` 将“用户明确选择的赛题编号、完整题名和对应基础要求”提升为硬门禁。在这些事实完整前，系统不再展示架构路线，不生成或批准计划，不调用外部模型继续猜测，也不生成 Agent Prompt。

## 现场证据

- 项目模式：`competition`
- 旧状态：`plan-ready`
- 旧 TaskSpec：`challengeId = challenge-1`
- 旧题名：与 `contestName` 完全相同，仅为整本赛事指南名
- 旧结构化内容：`basicTasks = []`、`advancedTasks = []`、`metrics = []`、`rubricItems = []`
- 旧分类：`fpga-cpu`
- 旧架构决策：`Cache + AI 加速冲刺路线`
- 用户对话中的 Leader 随后明确承认没有赛题编号或完整题目要求，证明咨询层与规划层此前使用了不一致的安全阈值

测试项目未被迁移或覆盖，原始错误状态保留作为诊断证据。

## 根因

1. 标题解析器只识别“选题一：”，没有识别指南使用的 `【赛题一】` 格式。
2. 解析失败后，旧逻辑把整本指南回退为一个虚构的 `challenge-1`。
3. 分类器扫描整本指南，遇到 RISC-V/CPU 关键词后错误归类为 CPU 题。
4. UI 默认选中第一个候选，并允许用户继续确认板卡与环境。
5. TaskSpec 完整性校验只检查通用 Requirement/Deliverable/Acceptance，没有校验具体题号、题名、基础要求和评分项。
6. 冲刺策略问题不是阻塞项；后续架构、计划和 Prompt 阶段没有重复验证赛题身份。

## 修复

- 支持 `【赛题一】`、`【选题一】`、`赛题一`、`选题一` 和 `【自由选题】`。
- 去除“整本指南自动当作第一题”的回退行为；零候选时导入按钮不可继续。
- 新增 `selectionConfirmed`，必须来自用户在候选题目列表中的明确选择。
- 新增赛题身份一致性检查：题目必须存在于已展示候选中，题名不能等于赛事名，基础要求和评分映射不能为空。
- 图形渲染加速器独立归类为 `fpga-accelerator`，优先级高于 RISC-V/CPU 关键词。
- 支持 `【基础要求】/【高阶挑战】` 与 `【赛题要求】+任务 1/2/3` 两种正文结构。
- 冲刺策略改为阻塞问题。
- 在 TaskSpec 批准、架构推荐、架构记录、计划生成、计划批准、Leader 咨询和 Agent Prompt 生成前重复执行身份门禁。
- 旧项目打开后显示阻塞诊断，不自动修复或覆盖历史记录。

## 官方指南回归结果

对测试项目中保存的易灵思 PDF 重新解析，得到 5 个明确候选：

1. AI 赋能的「实时绘画」——风格化视频渲染系统
2. 基于 RISC-V 与 FPGA 异构架构的 2D 图形渲染加速引擎
3. 多模态 AI 智能终端
4. 基于 FPGA 的实时图像边缘检测系统
5. 基于易灵思 FPGA 的开放创新设计

其中赛题二现在归类为 `fpga-accelerator`，解析到 3 个基础任务、3 个高阶任务、10 个指标和 16 个评分映射；全部 5 道题均通过赛题身份一致性检查。

## 自动化验证

- TypeScript 构建通过
- 15 个测试文件通过
- 62 个测试通过
- 新增覆盖：括号式赛题标题、目录/正文去重、未知题目停止、错误 challenge ID 停止、任务式赛题、旧项目未确认门禁、图形加速器不再误判 CPU

